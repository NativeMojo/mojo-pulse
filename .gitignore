.build/
.swiftpm/
Package.resolved
*.app
.DS_Store
xcuserdata/

# Distribution artifacts (DMGs, build staging)
dist/

# Auto-incrementing build counter (local to each developer / machine)
.build-number

# macOS resource files
*.swp

# Claude Code local state (launch.json is shared dev config and IS tracked)
.claude/worktrees/
.claude/settings.local.json
